# Temporary maintenance workflow retired after the scraper performance patch landed successfully.
# Kept intentionally inert so it cannot run again.
name: Retired Performance Fix

on:
  workflow_dispatch:

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Performance patch already applied."
